# 再構築計画: 野球スコアブックアプリ

作成日: 2026-07-26
対象: 本リポジトリの現状コードを踏まえた、完成に向けた開発計画。
前提資料: [docs/prd.md](prd.md)（要求は基本的に有効。本計画は PRD を置き換えるものではなく「どう作るか」を定める）

---

## 1. 現状評価（サマリー）

### 資産として残す価値が高いもの

| 資産 | 評価 |
|---|---|
| **PRD**（docs/prd.md） | 要求・データモデル・ルールが具体的で、そのまま設計の基準にできる |
| **UI コンポーネント群**（components/） | モバイルファーストで作り込み済み。bottom sheet 入力、セーフエリア対応、sticky 打順表、進塁シートのプレビュー等は再利用価値が高い |
| **スコアブック記法**（"遊ゴロ"・"盗2"・"WP·2→3" 等の生成ロジック） | ドメイン知識の蓄積。仕様として抽出する価値あり |
| **イベントソーシングの方針** | `events[]` を真実のソースとする設計方針自体は正しい |

### 根本的に作り直すべきもの

**ドメイン層（lib/）はイベントソーシングが不徹底で、これが主要バグの共通原因になっている。**

- `GameEvent` に導出値（`inning` / `half` / `team` / `runsScored` / `outsInPlay`）が**入力時の値で凍結保存**され、再計算で更新されない
  → 過去イベントの編集・削除でイニングスコアと打順表が実際の進行と乖離する（game-reducer.ts:65-103）
- 得点集計が二重基準：チーム得点は凍結値 `runsScored`、個人成績は `runnerMovements` を無条件カウント
  → 3アウト目と同時のホームインで**チーム合計と個人合計が合わない**（game-utils.ts:540 vs :593-609）
- `getEffectiveTotalInnings` が現在イニングを最大値に含めるため、**ビジター勝利・延長決着で試合が自動終了しない**（game-utils.ts:64, :939-960 の判定が事実上デッドコード）
- 走者の重複バリデーションがなく、野選・打撃妨害で一塁走者が**盤面から消える**（game-utils.ts:338-345, :462）
- `game-utils.ts`（960行）にルールエンジン・再生・統計・表示フォーマット・UI セレクタの6責務が混在
- 打点等の構造化データを `"右安·打点1"` のような**自由テキストに退化させ、正規表現で再パース**している
- **テスト0件**（AGENTS.md は TDD/vitest を指示しているが、vitest 自体が未導入）

### 主な機能ギャップ（PRD 比・野球ルール比）

- イニング数設定（PRD 1.2）が未実装 — `totalInnings: 9` ハードコード。**7回制の草野球が記録できない**
- 振り逃げが入力不可能（三振は強制アウト＋進塁シートスキップ）
- 犠飛と犠打の区別がなく、犠飛の既定進塁が誤り
- 併殺は「打者＋一塁走者」の1パターンのみ
- 代打・代走・投手交代・守備変更の概念が型にも UI にもない
- コールドゲーム未対応
- 走塁イベントが1走者固定（ダブルスチール不可）、走塁イベントの編集 UI は削除のみ
- 試合中画面に Undo ボタンがない（修正は打順表のセルを探して操作するしかない）
- localStorage にスキーマバージョン／マイグレーション機構がない

（詳細なバグ・ギャップ一覧は付録 A を参照）

---

## 2. 方針判断: 全面作り直しではなく「ドメイン層の再構築 + UI の流用」

| 選択肢 | 判断 |
|---|---|
| A. 現行コードにパッチを重ねる | ❌ バグの多くが「導出値の凍結保存」という設計に起因し、個別修正では再発する |
| B. リポジトリごと作り直す | ❌ UI・モバイル対応・記法ロジックという最大の資産を捨てることになる |
| **C. lib/ を TDD で再設計し、components/ は新ドメイン API に順次つなぎ替える** | ✅ **採用**。バグの根を断ちつつ資産を活かせる。テストが先にできるため移行の安全網にもなる |

方針 C の核となる設計判断:

1. **イベント＝入力のみ**。`GameEvent` が持つのは「誰が・どんな結果で・走者がどう動いたか」だけ。
   イニング・表裏・アウト・得点・打点集計は**すべて replay（先頭からの再生）で導出**する。
   → 編集・削除・挿入がどこで起きても整合性が自動的に保たれる（PRD 4.1 の再計算要求を構造で満たす）。
2. **ドメイン層は純関数のみ**（React・localStorage 非依存）。TDD が容易になり、AGENTS.md の方針とも一致。
3. **規定イニング数と到達イニング数を別概念にする**。試合終了判定のバグの根本原因を除去。
4. **走者の重複・3アウト整合などの不変条件を replay 内で検証**し、違反時は入力を拒否 or 警告を返す。
5. **表記（スコアブック記法）は構造化データから一方向に生成**。自由テキストの再パースを廃止。

---

## 3. 目標アーキテクチャ

```
lib/
├─ domain/
│   ├─ types.ts        // Game, Team, Player, GameEvent(入力のみ), Snapshot(導出状態)
│   ├─ rules.ts        // 既定進塁の提案、進塁の妥当性検証（純関数）
│   ├─ replay.ts       // events[] → Snapshot / タイムライン（イニング区切り・得点・成績を一括導出）
│   ├─ stats.ts        // 個人・チーム成績（replay の結果からのみ計算）
│   └─ notation.ts     // スコアブック表記の生成（"遊ゴロ" "盗2" 等）※逆パースはしない
├─ storage/
│   └─ local-storage.ts // schemaVersion 付き保存・読込・旧データのマイグレーション
└─ app-state/
    └─ game-context.tsx // useReducer + Provider（アクションは domain 関数の薄いラッパー）
```

### イベントスキーマ v2（要点）

```ts
// イベントは「入力」だけを持つ。位置づけ（何回の表か等）は replay が決める。
type GameEvent =
  | { id; kind: "atBat"; batterId; result: AtBatResult;
      battedBall?: { position: FieldingPosition; type: "ground" | "fly" | "liner" | "bunt" };
      movements: RunnerMovement[] }          // 打者含む全走者の移動。RBI フラグ付き
  | { id; kind: "baseRunning"; type: BaseRunningType;
      movements: RunnerMovement[] }          // 複数走者可（ダブルスチール対応）
  | { id; kind: "substitution";              // 代打・代走・守備交代・投手交代（Phase 3）
      team; inPlayerId; outPlayerId; role: "pinchHitter" | "pinchRunner" | "fielder" | "pitcher" }
  | { id; kind: "gameControl";               // コールド・没収など手動の試合制御
      action: "endGame"; reason?: string };
```

- `result` に `uncaughtThirdStrike`（振り逃げ）、`sacrificeFly`（犠飛）を追加し、`sacrifice` は犠打専用にする。
- 併殺・三重殺は専用 result をやめ、「movements 内のアウト数」から導出する（2-6-3 等の任意パターンに対応）。
- 打点は `RunnerMovement.isRBI` に一本化。`resultDetail` への埋め込み文字列（"·打点1"）は廃止。
- 保存データに `schemaVersion` を付与し、現行データ（v1）からのマイグレータを用意する。

### Replay（再生）の責務

`replay(events, config) => { snapshot, timeline, violations }`

- snapshot: 現在のイニング・表裏・アウト・走者・次打者
- timeline: イベントごとに「何回の表裏で起きたか・得点・アウト」を付与した列（スコアボード・打順表・成績はすべてここから描画）
- violations: 走者重複・アウト超過などの不変条件違反（編集時の警告表示に使用）

---

## 4. フェーズ計画

### Phase 0: テスト基盤（半日）

- [ ] vitest 導入（`pnpm add -D vitest`、`test` スクリプト追加）
- [ ] 現行 `lib/dummy-game.ts` の 42 打席シナリオを**特性テスト（characterization test）として移植**
      — 新ドメイン実装の受け入れ基準を先に固定する

### Phase 1: ドメイン層の再構築（TDD・最重要）

- [ ] `domain/types.ts` — イベントスキーマ v2
- [ ] `domain/replay.ts` — 3アウトチェンジ、打順循環、押し出し、得点の3アウト同時判定、走者重複検証
- [ ] `domain/rules.ts` — 打席結果ごとの既定進塁提案（現行 `getDefaultMovements` の仕様をテストに写してから再実装。犠飛の既定進塁は修正）
- [ ] **試合終了判定の修正**: 規定イニング（設定値）と到達イニングを分離し、ビジター勝利・サヨナラ・延長決着を正しく判定（テスト先行で現行バグ (a) を再現→修正）
- [ ] `domain/stats.ts` — 個人・チーム成績を timeline からのみ算出（二重基準バグの解消）
- [ ] `domain/notation.ts` — 現行 3 箇所（at-bat-notation.ts / game-utils.ts 表示系 / types.ts ラベル定数）に分裂した表記ロジックを統合

**完了条件**: dummy-game 特性テスト + 付録 A のバグ再現テストが全て green。UI には未接続。

### Phase 2: UI つなぎ替えとデータ移行

- [ ] `storage/local-storage.ts` — schemaVersion + v1→v2 マイグレータ（既存の保存試合を壊さない）
- [ ] `game-context.tsx` のアクションを新ドメインに差し替え（コンポーネントの props 変更は最小限に）
- [ ] `live-scoring.tsx` に直書きされたイベント組み立てロジック（:54-167）をドメインのファクトリ関数に置換
- [ ] 打順表・スコアボード・成績表の描画元を timeline に統一
- [ ] 動作確認: セットアップ→7回試合→結果表示→過去打席の編集→再計算、を実機（スマホ幅）で通し確認

**完了条件**: 現行と同等の機能が新ドメインの上で動く。編集後の表示崩れ（バグ (c)）が解消。

### Phase 3: 機能ギャップの解消（PRD 完全対応 + ルール拡充）

優先度順:

- [ ] **イニング数設定**（5/7/9/カスタム）— PRD 1.2。草野球アプリとして必須
- [ ] **試合中 Undo ボタン**（直前イベントの取り消し）— PRD 画面2 のレイアウトに明記あり
- [ ] **振り逃げ**（uncaughtThirdStrike: 三振記録 + 打者一塁到達）
- [ ] **犠飛**（sacrificeFly の追加、犠打との集計区別、打数除外ルール）
- [ ] **任意パターンの併殺・複数アウト**（movements ベース化により進塁シートで表現可能にする）
- [ ] **複数走者の走塁イベント**（ダブルスチール等）と**走塁イベントの編集**（現状は削除のみ）
- [ ] **代打・代走**（substitution イベント。打順スロットに出場履歴を持たせる）
- [ ] **コールドゲーム・試合終了理由の記録**（gameControl イベント）
- [ ] 編集時に手動指定した進塁が既定値に戻る問題の解消（編集フローでも進塁シートを開く）

### Phase 4: 磨き込み（任意・PRD の Next Phase から）

- [ ] トースト等の操作フィードバック（Toaster が未マウントのまま）
- [ ] 試合ログのエクスポート（JSON / テキスト共有）
- [ ] 試合履歴（localStorage 内で複数試合を保持）
- [ ] 投手成績（現状 `startingPitcherName` は入力だけあって未使用。代打対応後に着手）
- [ ] PWA / オフライン明示対応

### 併せて行うクリーンアップ

- [ ] 未使用コードの削除: `theme-provider.tsx`（未マウント）、`hooks/use-mobile.ts`・`hooks/use-toast.ts`（参照0件）、`game-utils.ts` の未使用エクスポート群（`getDefaultOuts` 等）
- [ ] `batting-order.tsx` と `batting-scorebook-table.tsx` に二重実装された「打順×イニング表」の共通化
- [ ] `game-setup.tsx` の並べ替え（HTML5 DnD はタッチ非対応）→ ▲▼ を正とするか touch 対応ライブラリ導入
- [ ] `at-bat-notation.ts` の「内野安 = otherOut（アウト扱い）」の誤登録修正

---

## 5. テスト戦略

- **ドメイン層**: vitest による純関数の単体テスト（TDD、t-wada 流: 仮実装→三角測量→一般化）。
  - シナリオテスト: dummy-game 42 打席の特性テスト + 「押し出しサヨナラ」「延長」「ビジター勝利」「3アウト同時ホームイン」「編集→再計算」の各シナリオ
  - 付録 A の各バグは**先に失敗するテストを書いてから**修正する
- **UI**: 手動の通し確認を各 Phase の完了条件に含める（コンポーネントテストは費用対効果を見て Phase 3 以降で判断）
- **移行**: v1 の実データ（現在の localStorage 形式）を fixture 化し、マイグレータのテストに使う

---

## 6. リスクと対応

| リスク | 対応 |
|---|---|
| ドメイン再設計が肥大化して完成が遠のく | Phase 1 の完了条件を「特性テスト green」に固定し、機能追加は Phase 3 まで凍結 |
| v1 保存データの移行漏れ | 凍結導出値は捨てて replay で再導出するため、移行対象は入力フィールドのみ＝小さい |
| 進塁 movements の自由度を上げると入力 UX が複雑化 | 既定値提案（rules.ts）を強化し「ほとんどの打席は確定1タップ」を維持。PRD 成功指標「1打席10秒以内」を基準に判断 |
| 現行挙動の意図しない変更 | 特性テストで現行仕様を先に固定。意図的に変える箇所（犠飛の既定進塁等）はテスト側を明示的に更新 |

---

## 付録 A: 現行コードの既知バグ一覧（Phase 1-2 で再現テストを書く対象）

| # | 内容 | 場所 |
|---|---|---|
| a | ビジター勝利・延長決着で試合が自動終了しない（`getEffectiveTotalInnings` が現在回を含むため終了判定がデッドコード化） | game-utils.ts:64, 939-960 |
| b | 野選・打撃妨害で一塁走者が盤面から消える（走者重複の検証なし） | game-utils.ts:338-345, 462 |
| c | 過去イベントの編集・削除後、イベントに凍結された inning/half が更新されず表示が壊れる | game-reducer.ts:65-103 |
| d | 3アウト同時ホームインがチーム得点には入らないのに個人得点・打点には入る（二重基準） | game-utils.ts:540 vs 593-609 |
| e | 規定回超のイニングで `getInningScores` が NaN を伝播しうる | game-utils.ts:531-541 |
| f | 編集時に手動指定の進塁が既定値で上書きされる | game-utils.ts:858-896 |
| g | legacy `strikeout` が常に「空振三振」表示になる | types.ts:192 |
| h | 「内野安」が `otherOut`（アウト扱い）として登録されている | at-bat-notation.ts:53-57 |

## 付録 B: 現状の構成（参考）

- Next.js 16 (App Router) + React 19 + Tailwind 4 + shadcn/ui。単一ページ（URL ルーティングなし）で `setup / live / finished` の3状態を切替
- 状態管理: React Context + useReducer、localStorage 自動保存（キー `baseball-scorer-game`）
- アプリコード約 4,000 行（うち lib/ 1,600 行、components/ 直下 3,100 行）。components/ui/ は shadcn 生成物
